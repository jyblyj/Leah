<block wx:if="{{$taroCompReady}}">
    <block wx:if="{{anonymousState__temp}}">
        <view class="header" style="{{_$anonymousState__temp}}">
            <view style="{{_$anonymousState__temp2}}"><text class="title">{{title}}</text>
            </view>
        </view>
    </block>
</block>